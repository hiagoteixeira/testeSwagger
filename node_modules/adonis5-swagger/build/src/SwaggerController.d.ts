/// <reference types="@adonisjs/http-server/build/adonis-typings" />
import { IocContract } from '@adonisjs/fold/build';
import { HttpContextContract } from '@ioc:Adonis/Core/HttpContext';
export declare class SwaggerController {
    protected IoC: IocContract;
    private config;
    private swaggerFileContent;
    constructor(IoC: IocContract);
    swaggerUI({ params, response }: HttpContextContract): Promise<void>;
    swaggerFile(): Promise<object | null | undefined>;
    private getSwaggerSpecFileContent;
}
